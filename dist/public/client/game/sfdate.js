//(5/4/22) a date is identified by the server as just the offset from the start of the game
export function numberToDate(num) {
    return '';
    //return `${date.getMonth() + 1}/${date.getDate()}/${date.getFullYear()}`;
}
